{% extends "base.html.twig" %}

{% block title %}
	{{ category.name}}
{% endblock %}

{% block body %}

	<main class="contrainer">
		<section class="row">
			<div class="mb-4">
				<h1 class="d-flex justify-content-center">{{ category.name}}</h1>
			</div>
			{% for film in films %}
				<div class="col-4">
					<div class="item">

						<a href="{{ path('films_details_', {"slug": film.slug}) }}" class="card-link">
							<img src="\images\image1-min.png" class="image" width="100%" height="200" aria-label="Placeholder: Image cap" focusable="false" role="img">

						</a>
						<div class="middle">
							<div class="title">{{ film.titre }}</div>
						</div>

					</svg>
				</div>
			</div>
		{% endfor %}
		{# {% set path = 'format_list' %}
																																{% set slug = format.slug %}
																																{% set pages = films.pages %}
																																{% set currentPage = films.page %}
																																{% include "partials/pagination.html.twig" %} #}

	</section>
</main>{% endblock %}
